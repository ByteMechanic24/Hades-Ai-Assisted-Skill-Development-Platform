ai-service {
  base-url = "http://localhost:8000"
  base-url = ${?AI_SERVICE_URL}
}

http {
  interface = "0.0.0.0"
  interface = ${?HTTP_INTERFACE}
  port = 8080
  port = ${?HTTP_PORT}
}

database {
  driver = "org.postgresql.Driver"
  url = "jdbc:postgresql://127.0.0.1:15432/hcl_learning"
  url = ${?DATABASE_URL}
  user = "hcl"
  user = ${?DATABASE_USER}
  password = "hcl_dev_password"
  password = ${?DATABASE_PASSWORD}
  numThreads = 10
  maxConnections = 10
}

jwt {
  secret = "hades-super-secret-jwt-signing-key-2026"
  secret = ${?JWT_SECRET}
}

oauth {
  google {
    client-id = "1053667687662-gr9tddp9th9nce87ff8o7fqrefbhnqcf.apps.googleusercontent.com"
    client-id = ${?GOOGLE_CLIENT_ID}
    client-secret = "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"
    client-secret = ${?GOOGLE_CLIENT_SECRET}
  }
}

pekko {
  http {
    server {
      request-timeout = 300 s
      idle-timeout = 360 s
    }
    client {
      connecting-timeout = 60 s
      idle-timeout = 360 s
    }
    host-connection-pool {
      idle-timeout = 360 s
      response-entity-subscription-timeout = 300 s
    }
  }
}

